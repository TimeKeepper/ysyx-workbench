#include <utils.hpp>
#include <main.hpp>
#include <iostream>

int main(int argc, char **argv) {

  Log("REBUILD");

  // init_monitor(argc, argv);

  // engine_start(argc, argv);

  // return is_exit_status_bad();
}
